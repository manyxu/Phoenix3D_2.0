-- boost.lua
-- this file is then engine start called file, it's the entry point of logic

--DebugText:SetText("phoenix3d scriptsystem ok.")
UI = Project:GetUI()

-- begin system fun
-- these funs will be called by engine, so dont't remove them
-- todo: these funs can be registed to engine, otherwise, will not be called
function OnUpdate (appTime, elapsedTime)
end

function OnTouchPressed(x, y)
end

function OnTouchMoved(x, y)
end

function OnTouchReleased(x, y)
end

function OnTouchCancelled(x, y)
end
-- end system fun